<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="quini.com.quinimania" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="quini.com.quinimania.MainActivity" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JugarActivity" />
        <activity android:name=".MisJugadasActivity" />
        <activity
            android:name=".UltimoSorteoActivity"
            android:label="@string/title_activity_ultimo_sorteo" >
        </activity>
        <activity
            android:name=".LosSalidoresActivity"
            android:label="@string/title_activity_los_salidores" >
        </activity>
    </application>

</manifest>
